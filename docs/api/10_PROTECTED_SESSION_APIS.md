# Protected Session APIs

## POST /api/v1/assets/{assetId}/protected-session
Issue protected session after access evaluation (see `docs/protection/documents/01_PROTECTED_DOCUMENT_FLOW.md`).

## DELETE /api/v1/protected-content/session/{sessionId}
Manually close/invalidate a session.
