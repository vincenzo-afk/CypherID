# Video APIs

## POST /api/v1/videos/{videoId}/session
Start protected video playback session.

## GET /api/v1/videos/chunk
Retrieve video chunk via protected session (delegates to protected-content chunk API).
