# Exam Rendering

Exam always uses at least HIGH protection profile.
Fullscreen enforced via Fullscreen API.
Question text rendered on Canvas (ProtectedRenderer).
Answer inputs are standard HTML form elements (not protected renderer).
