# Supported Capture Events

Events the system monitors and responds to:
1. visibilitychange (tab hidden)
2. blur (window focus lost)
3. beforeprint (print dialog)
4. fullscreenchange (exam mode)

All events documented with actual browser API. No invented capabilities.
