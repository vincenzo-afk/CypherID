# Logging

Structured JSON logs via Logback (Java) and standard logging (Python). Correlation ID (`X-Request-ID`) propagated across services. No sensitive data logged — see `docs/development/08_SECURITY_CODING_RULES.md`.
