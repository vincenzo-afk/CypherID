# Key Rotation

1. Generate new key
2. Deploy alongside old key (dual-key period)
3. Re-encrypt/re-sign with new key
4. Retire old key after grace period

See `docs/security/09_KEY_MANAGEMENT.md` for rotation schedule per key type.
