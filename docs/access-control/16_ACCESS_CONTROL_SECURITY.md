# Access Control Security

All evaluation in chaincode (cannot be bypassed by application layer).
Every decision recorded on-chain (cannot be tampered with).
Re-evaluation on every request (no cached grant state — Redis caches for performance, but blockchain is authoritative).
