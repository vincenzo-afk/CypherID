# Resource Protection

Every protected resource must have an AccessPolicy on-chain.
Access evaluation fails (DENIED) if no policy exists for a resource.
Resource ID is the asset ID (on-chain) or document ID (protected content).
