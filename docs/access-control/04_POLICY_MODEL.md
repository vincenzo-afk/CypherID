# Policy Model

See `docs/blockchain/14_ACCESS_CONTROL_CHAINCODE_SPEC.md` for AccessPolicy JSON model.
Policies created by org admins. Applied to specific resourceIds.
Policies are versioned; old versions retained for audit.
