# DID Creation Workflow

See `docs/workflows/01_USER_ONBOARDING.md` — DID creation is part of onboarding.
Standalone DID update (new key): `PUT /api/v1/identity/did/{did}` with new public key + signature.
