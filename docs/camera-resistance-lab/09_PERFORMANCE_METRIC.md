# Performance Metric

Measured via browser Performance API:
- Rendering FPS (requestAnimationFrame timestamps)
- CPU utilization (not directly available; estimated via frame timing)
- Memory usage (performance.memory where available)

Target: 60 FPS at MEDIUM profile on mid-range laptop.
