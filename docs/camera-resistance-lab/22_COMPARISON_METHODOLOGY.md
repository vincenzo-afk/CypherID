# Comparison Methodology

Compare each configuration to baseline.
Report: absolute OCR accuracy + relative reduction from baseline.
Example: 'HIGH profile reduced OCR accuracy from 0.87 (baseline) to 0.31 (64% reduction) under standard conditions.'
