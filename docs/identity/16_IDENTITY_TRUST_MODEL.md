# Identity Trust Model

Root of trust: Fabric CA per organization.
Org admin controls who gets enrolled.
DID trust: anyone can resolve; only owner can update (with private key).
VC trust: only authorized issuers can issue (enforced by chaincode).
