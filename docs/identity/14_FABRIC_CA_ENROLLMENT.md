# Fabric CA Enrollment

User KYC approved → Identity Service calls Fabric CA SDK.
CA issues X.509 certificate for user.
Certificate stored in Fabric wallet (server-side).
Used for: submitting chaincode transactions on behalf of user.
